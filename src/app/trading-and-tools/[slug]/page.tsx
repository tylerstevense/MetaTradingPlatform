
const MetaData5 = ({ params }: { params: { slug: string } }) => {
    return (
        <div>
            <h1>{params.slug} Hello Jello Mello Kello</h1>
        </div>
    );
};

export default MetaData5;